# Check Make GUI concept v4 — generation prompt

Built-in image generation edited GUI concept v3 with the canonical Check Make symbol and wordmark as identity references.

## Prompt

Create a precise, high-fidelity update of Check Make GUI concept v3. Preserve the macOS window, canonical branding, project-context panel, 3D viewport, mounting bracket, build plate, model tools, recommendation hierarchy, typography, colors, icons, and spacing.

Replace the top navigation with a centered, state-driven process indicator that cannot be mistaken for tabs:

- Step 1 uses a Make Green circle with a white checkmark, `Inspect`, and secondary status `Complete`.
- A completed green connector leads to step 2.
- Step 2 uses a larger Action Green outlined circle containing `2`, with `Prepare` and secondary status `2 decisions left`. This is the active step.
- A muted connector leads to step 3.
- Step 3 uses a light-gray outlined circle with a lock, `Export`, and secondary status `Locked`.

Retain the settings gear at upper-right and the canonical `CHECK / MAKE` identity at left. Keep one continuous workspace rather than separate pages.

In the right panel, change the heading to `Prepare print`, retain `Review required`, `Ready after 2 decisions`, the recommendations, uncertainty warning, `Evidence & rationale`, and key settings. Use the primary action `Review 2 decisions`.

Keep the Export project area visible but disabled and muted. Show a lock with `Complete preparation to unlock`, and show all Core 3MF and slicer rows as unavailable until preparation is complete.

Keep the bottom status strip: `Local geometry measured`, `Evidence available`, and `No structural simulation`.

Follow the canonical Check Make palette: Make Green `#35C98B`, Action Green `#168866`, Blueprint Navy `#073A60`, Technical Cyan `#8DDCFF`, Technical Ink `#171A19`, Warm White `#F7F7F2`, and Cool Canvas `#F3F5F4`.

The process indicator represents state derived from input and decisions, not navigation. The result remains a static, non-functional, premium native macOS mockup. Do not add a black title bar, navigation tabs, fake readiness score, FEM claims, mesh-repair claims, browser chrome, or watermark.
