# Check Make GUI concept v3 — generation prompt

Built-in image generation edited GUI concept v2 with the canonical Check Make symbol and wordmark as identity references.

## Prompt

Create a precise, high-fidelity update of Check Make GUI concept v2. Preserve the window proportions, canonical branding, warm-white title bar, project-context cards, 3D viewport, build plate, recommendation panel, export panel, typography, colors, and spacing.

Apply these changes:

1. Replace the five top navigation labels with exactly three action-oriented labels: `Inspect`, `Prepare`, and `Export`. Show `Inspect` selected with an Action Green underline.
2. Add a small outlined settings gear at the far upper-right of the title bar.
3. Remove `Open project…` and `Saved` from the left project panel; leave clean whitespace beneath the MODEL, PRINTER, MATERIAL, and ANALYSIS cards. Project opening and saving belong to the native macOS application menu.
4. Add a collapsed `Evidence & rationale` disclosure directly beneath the uncertainty warning and above Key settings.
5. Keep `Intended load direction needs confirmation` where the decision is presented.
6. Keep `Selected`, `Risk map`, and `Compare` directly above the 3D viewport.
7. Change the primary action from `Review recommendation` to `Prepare print`.
8. In the bottom status strip, use `Local geometry measured`, `Evidence available`, and `No structural simulation`.

Preserve the canonical checked-cube symbol and `CHECK / MAKE` wordmark. Follow the Check Make palette: Make Green `#35C98B`, Action Green `#168866`, Blueprint Navy `#073A60`, Technical Cyan `#8DDCFF`, Technical Ink `#171A19`, Warm White `#F7F7F2`, and Cool Canvas `#F3F5F4`.

The result remains a static, non-functional, premium Figma-quality native macOS utility mockup. Do not add a black title bar, sidebar navigation, generic cube logo, fake readiness score, FEM claims, mesh-repair claims, browser chrome, or watermark.
